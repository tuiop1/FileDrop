# Copy this file to .env.dev. The values are for local development only.

POSTGRES_PASSWORD=postgres

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
FILEDROP_S3_ACCESS_KEY=filedrop
FILEDROP_S3_SECRET_KEY=filedrop-local-secret

FILEDROP_BACKEND_URL=http://host.docker.internal:8080
FILEDROP_PROXY_TIMEOUT=15m
FILEDROP_LOG_LEVEL=DEBUG
